## What does this change do?

<!-- Short description. Link the issue it closes: Closes #123 -->

## Which package(s) does it touch?

<!-- schema / mcp-server / compiler-slidev / compiler-hyperframes / cli / docs / ci -->

## Checklist

- [ ] `pnpm build && pnpm lint && pnpm typecheck && pnpm test` pass locally
- [ ] Added a changeset (`pnpm changeset`) if this changes published package behavior
- [ ] Updated relevant docs in `docs/` if this changes public behavior
- [ ] If AI-assisted and not yet reviewed line-by-line by a human, this PR is labeled `status: needs-human-review`

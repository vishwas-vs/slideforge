{
  "name": "@slideforge/schema",
  "version": "0.0.0",
  "private": true,
  "description": "Shared content model (Deck/Beat/Block), validation, and patch-based versioning used by both the Slidev and HyperFrames compilers.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "lint": "biome lint .",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}

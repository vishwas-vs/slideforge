// Placeholder entry point for @slideforge/compiler-hyperframes.
// Real content — Deck -> HyperFrames composition generation via @hyperframes/core,
// plus catalog-block selection — lands in tracked v0.4 issues (see
// docs/video-compiler.md once written).
export const COMPILER_HYPERFRAMES_PACKAGE_PLACEHOLDER = true;

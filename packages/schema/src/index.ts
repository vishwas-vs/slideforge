// Placeholder entry point for @slideforge/schema.
// Real content — the Deck/Beat/Block zod schema and patch-based versioning
// engine — lands in tracked v0.2 issues (see docs/content-model.md once written).
export const SCHEMA_PACKAGE_PLACEHOLDER = true;

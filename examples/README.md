# Examples

Sample `Deck` content-model fixtures and their compiled output (Slidev project / HyperFrames composition) will land here as part of the v0.2 milestone, once `packages/schema` and the compilers exist.
